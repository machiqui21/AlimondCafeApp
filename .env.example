# Copy to .env and adjust as needed
# Common setups:
# - XAMPP: usually port 3306
# - WAMP: often port 3307
# - MySQL Community on Windows Service (MySQL80): default 3306

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=alimondcafe

# Optional: enable verbose logs
# VERBOSE=1
# To disable DB persistence during dev (session-only cart)
# PERSIST_ORDERS=0
