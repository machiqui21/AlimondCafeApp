<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Registration Successful • Alimond Café</title>
  <link rel="stylesheet" href="/styles.css">
  <%- include('partials/navbar_login.ejs') %>
  <script type="text/javascript">
    function googleTranslateElementInit() {
      new google.translate.TranslateElement({
        pageLanguage: 'en',
        includedLanguages: 'en,tl',
        layout: google.translate.TranslateElement.InlineLayout.SIMPLE
      }, 'google_translate_element');
    }
  </script>
  <script type="text/javascript" src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>
  <style>
    .auth-card { max-width: 640px; margin: 32px auto; padding: 32px; border: 1px solid #e9e9e9; border-radius: 14px; box-shadow: 0 10px 28px rgba(0,0,0,0.08); background:#fff; }
    .auth-card h2 { margin: 0 0 18px; font-size: 26px; }
    .kv { display: grid; grid-template-columns: 160px 1fr; gap: 10px 16px; margin: 10px 0 18px; }
    .kv .k { color:#555; }
    .cta { display:flex; gap:10px; margin-top: 12px; }
    .btn { height: 42px; padding: 0 16px; border-radius: 10px; border: none; background: #6b4a2c; color: #fff; font-size: 15px; cursor: pointer; text-decoration:none; display:inline-flex; align-items:center; }
    .btn.alt { background:#eee; color:#333; }
    .meta { color:#666; font-size: 13px; }
  </style>
</head>
<body>
  <div style="background: linear-gradient(135deg, #8b5a3c 0%, #d4a373 100%); padding: 12px 20px; display: flex; justify-content: flex-end; align-items: center; flex-wrap: wrap; gap: 10px; box-shadow: 0 2px 8px rgba(0,0,0,0.1); margin-bottom: 10px;">
    <div style="display: flex; align-items: center; gap: 15px; flex-wrap: wrap;">
      <span style="color: white; font-weight: 600; font-size: 1rem;">🌐 Select Language / Pumili ng Wika:</span>
      <div id="google_translate_element" style="background: rgba(255,255,255,0.2); padding: 6px 12px; border-radius: 6px;"></div>
    </div>
  </div>
  <header class="hero">
    <img class="logo" src="/images/AlimondsCafe.jpeg" >
    <h1><%= __('Welcome to Alimond\'s Café') %></h1>
  </header>

  <div class="auth-card">
    <h2>Registration Successful</h2>
    <p class="meta">Your account has been created. Here are your details:</p>
    <div class="kv">
      <div class="k">First Name</div>
      <div class="v"><%= (user && user.FirstName) ? user.FirstName : '' %></div>
      <div class="k">Last Name</div>
      <div class="v"><%= (user && user.LastName) ? user.LastName : '' %></div>
      <div class="k">Username</div>
      <div class="v"><%= (user && user.Username) ? user.Username : '' %></div>
      <div class="k">Email</div>
      <div class="v"><%= (user && user.Email) ? user.Email : 'Not provided' %></div>
      <div class="k">Mobile Phone</div>
      <div class="v"><%= (user && user.MobilePhone) ? user.MobilePhone : '' %></div>
      <div class="k">Created</div>
      <div class="v"><%= (user && user.CreatedAt) ? user.CreatedAt : 'Just now' %></div>
    </div>
    <div class="cta">
      <a class="btn" href="/login">Go to Login</a>
      <a class="btn alt" href="/menu">Browse Menu</a>
    </div>
  </div>
  
  <div style="text-align: center; margin: 20px;">
    <button onclick="toggleDarkMode()" style="padding: 10px 20px; background: #6b4a2c; color: white; border: none; border-radius: 8px; cursor: pointer;">🌙 Toggle Dark Mode</button>
  </div>

  <script>
    function toggleDarkMode() {
      var isDark = document.body.classList.toggle('dark-mode');
      try { localStorage.setItem('darkMode', isDark ? '1' : '0'); } catch(e) {}
    }
    
    // Restore saved dark mode preference
    document.addEventListener('DOMContentLoaded', function() {
      try {
        var saved = localStorage.getItem('darkMode');
        if (saved === '1') document.body.classList.add('dark-mode');
      } catch(e) {}
    });
  </script>
</body>
</html>
